name: Build

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ["ubuntu-latest", "windows-latest"]
      fail-fast: false
    steps:
    - uses: actions/checkout@v2
    - name: Set Python Version
      uses: conda-incubator/setup-miniconda@v2
      with:
        auto-update-conda: true
        add-pip-as-python-dependency: true
        python-version: 3.6
    - name: Set environment
      run: |
        conda config --set always_yes yes
        conda install numpy scipy wheel
        conda list
    - name: Install
      run: |
        pip install coveralls coverage pytest pytest-cov sphinx sphinx_rtd_theme redbaron
        pip install .
    - name: Build and Test
      run: |
        python -m pytest --cov-config .coveragerc --cov=openconcept
    - name: Push to Coveralls
      if: ${{ matrix.os }} == "ubuntu-latest"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: coveralls --service=github --rcfile=.coveragerc;
