version: 2

sphinx:
  configuration: docs/conf.py

formats: 
  - htmlzip
  - epub

python:
  version: 3.6
  install:
    - method: pip
      path: .
      extra_requirements:
        - testing
  system_packages: true
